# Current CLI Wrappers

The authoritative current CLI list is
`config/current-cli-entrypoints-v1.json`. Existing supported entrypoints remain
at their registered paths and are not duplicated here. Future thin wrappers may
move into this directory only when the registry and tests move with them.

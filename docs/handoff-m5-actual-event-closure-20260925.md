# M5 actual event replay handoff (2026-09-25)

This branch adds an offline, fail-closed replay for the 600519 actual event receipts. It verifies five half-year financial facts against archived PDF bytes and announcement metadata, builds a versioned fact artifact, derives a bounded recalculation plan, and projects the result into an M7 workbook candidate. No order, production database, scheduler, notification, or account action is performed.

The two actual receipts remain `STILL_NOT_READY`: `valuation_inputs` is missing, `model_executed=false`, and `new_valuation_result=null`. The workbook is a candidate, not the published WPS original or a trading recommendation. The current v5 candidate SHA-256 is `ff4a1412b41b313e347b9177207caab8a498a2bf04dd31742cfb1b2858fc460a`.

Verification in this isolated checkout: 22 targeted tests passed and 6 evidence-dependent tests skipped. Eight pre-existing outbox fixture failures were traced to Windows CRLF conversion; the fixture now retains LF bytes and those eight tests pass. The full repository suite is not green here (`2244 passed, 44 skipped, 208 failed, 87 errors`), principally because the isolated checkout lacks ignored `runtime` research evidence used by unrelated tests. Do not treat that run as production acceptance.

Follow-up binding audit: the result validator now checks exact active-event coverage and the status/blockers/task set implied by the frozen plan, even when a forged result is rehashed. Both the evaluator and M7 reader bind the facts artifact to the graph node's file-byte SHA and PDF evidence. An isolated replay regenerated two `STILL_NOT_READY` outcomes and a nine-review M7 read model; the replay files remain ignored under `runtime/`.

With `M5_ACTUAL_EVIDENCE_ROOT` pointing at the existing local evidence archive, the scoped M5/M7 regression is `34 passed` with no skips. This does not supersede the separate full-repository result above.

The M7 event sheet now exposes affected dependencies, explicit recalculation blockers, Event ID, human Decision Review requirement and the absence of a new valuation. A v5 candidate was rebuilt and read directly through openpyxl; WPS/Excel COM read-only acceptance passed. The canonical WPS workbook remained byte-identical at SHA-256 `64c8deff1a237076d2ba0b00afc8905d23bd9d117cb132dfc6757071b5659911`. The exact `core-research-gates.yml` offline test command passed locally: `686 passed, 18 skipped` with the existing local actual-evidence root available.

The `valuation_inputs` gap is substantive, not merely an adapter naming issue. The retained 600519 `valuation_result` predates these verified event facts. The archived `runtime/valuation-assumptions/600519-current-latest.json` points to an assumption package explicitly marked `mapping_only=true`, `valuation_recalculated=false`, and `valuation_approved=false`; it is not a new model-input artifact. The shared residual-income route requires authenticated starting book equity and ordinary shares plus complete scenario inputs. The five newly verified H1 fields do not include equity or shares, **but these values are not absent from the archive**: `runtime/company-research/600519-consolidated-parent-equity-inputs-latest.json` pins a separate 2026-H1 evidence package with parent equity `251253594419.50` CNY and issued shares `1250081601`, both referencing the same original PDF SHA-256 `0e10aa26be46b1cf3cd03f06e834c7fb98d5dd0d661b96f8fddd4af7e846a4f6`. Its evidence file SHA-256 is `efc4dc37d81e9d4bf54930a5c2bf7cd50305b7b7ce808531ef4bc4138f80891b`. That package says `valuation_approved=false` and `as_of_registry_verified=false`; the values have not been ingested into a new event-bound ResearchInputDescriptor. Reusing the prior result or relabeling the mapping-only package would break provenance.

Acceptance audit (candidate scope, not production authorization): A1 nine hash-bound review decisions are shown by the reviewed queue and M7 read model; A2 the input is an ACTUAL durable-run receipt, not a synthetic fixture; A3 the graph maps real `financial_facts` and `valuation_result` while retaining the actual `valuation_inputs` gap; A4 five versioned PDF-backed H1 facts exist; A5 two events have explicit bounded `STILL_NOT_READY` results, with no model execution; A6 M7 candidate exposes dependency, blocker, event and review state; A7 an isolated cold apply of the archived ACTUAL request matches the archived receipt fingerprint, and a second run after reopening stores is an idempotent replay; A8 scoped tests, Core Gate, WPS and workbook hashes pass, while the unrelated full repository suite is not green in the isolated checkout; A9 no order or position instruction is produced; A10 no production or PTA action was performed. The replacement valuation-input contract and model refresh remain open; this is not a claim that a usable new valuation exists.

The bounded-result evaluator now regenerates the entire plan from the receipt and graph before accepting it, so dropping an invalidated dependency while preserving a self-consistent result hash is rejected. A successful bounded refresh is still unimplemented: when complete authenticated inputs eventually exist, the existing ResearchApplication/ValuationRouter path must execute and persist a distinct new ValuationResult and ModelValidity; changing a status string is not a substitute. This keeps Engineering DONE unclaimed.

The M7 read model now decodes each review through the existing EventMaterialityDecision contract and compares the decision title, archived source reference, materiality status, affected fields/artifacts, cluster/supersede identity and direct dependencies against the actual event. Adversarial source-URL and affected-field edits are rejected. Rebuilding from the archived nine decisions still yields nine reviewed items; the read-model replay SHA-256 is `efbc63098619e51e16d9b91e4dd0086fab29795d346761d302513709e1f4667b` (local ignored runtime output).

The archived equity/share package was independently cross-checked with the newer M5 fact package and original PDF bytes. The current-period equity/prior-period equity pair appears on physical page 28; the opening/change/ending share row appears on physical page 22, and the closing equity also appears in the page-37 rollforward. This confirms those observations are available for a future descriptor, not that the existing unapproved assumptions can be promoted into a current ValuationResult.

The shared ResearchRunSpec now accepts a research case date later than its financial facts date, matching the pre-existing ResearchInputDescriptor PIT frame. It still requires the valuation date to equal the typed facts date, availability not to precede the research date, and the research date not to precede the facts. A typed descriptor test executes the existing ResearchApplication with distinct dates and confirms the new outcome retains the research date while the ValuationResult retains the older financial basis. This removes a contract-level impediment to a future event-bound descriptor; it does not create approved scenario inputs or a usable 600519 valuation.

An offline adapter now builds a blocked `ResearchInputDescriptor` from the ACTUAL receipt, PDF-bound verified facts and separately pinned equity/share archive. The local write-once candidate at `runtime/m5-pending-research-input-actual-20260925.json` has file SHA-256 `b7d658ff44e39fbecebd7d08dd1f95ce97114e95bd0a3e4ff61611543b3aebae` and descriptor input SHA-256 `72ad1453eec81d8c6b9a5908614928316f3f94007507dbfc276cb87100a031e6`. The existing ResearchApplication/ValuationRouter reads it and returns `not_ready` with null bear/base/bull, specifically because the event research and scenario inputs are unapproved. This candidate is **not** registered as a satisfied `valuation_inputs` dependency node; the original bounded result remains `STILL_NOT_READY`, and no new usable valuation or decision is published.

Next engineering gate: independently reconcile the archived equity/share observations to their PDF pages and connect them with the verified period facts in the existing `ResearchInputDescriptor` contract; obtain reviewed, PIT-consistent scenario assumptions without treating the mapping-only archive as approval. Route through the existing unified valuation interface; verify that any new result is tied to the actual event and facts before changing decision presentation. Until then `STILL_NOT_READY` and no new valuation are the correct outcome. Keep the formal workbook unchanged.

The pending-input adapter now requires the verified facts' PDF SHA-256, source URL and announcement ID to identify exactly one active event in the ACTUAL receipt. Three rehashed facts-package tampering cases are rejected; a same-symbol but unrelated filing can no longer enter this preflight. Commit `7eea874` contains the scoped change. The workflow's offline Core Gate passed `689 passed, 18 skipped` with `M5_ACTUAL_EVIDENCE_ROOT=D:\GPTProject\value-investment` and pytest's temporary directory explicitly placed under this isolated worktree. The default Windows temp directory was inaccessible (`WinError 5`); the initial `150 errors` were fixture setup errors, not test assertion failures. The formal workbook and original bounded `STILL_NOT_READY` result are unchanged. A successful bounded refresh remains unimplemented and cannot be inferred from this gate.

## Subsequent bounded-refresh engineering

The last sentence above is superseded for the **synthetic complete-input path only**. Commits `c5ac839` through `756c06d` compute source hashes from exact bytes, reject mismatched typed-facts/thesis graph bindings, attach a `valuation_inputs` node only after event/PDF/receipt and model-input preflight checks, and execute a bounded refresh through the existing `ResearchApplicationService`. The result receipt pins the frozen plan, prior source-file bytes, prior persisted artifact, new persisted valuation version and current repository head. Its validator rejects a rehashed false `RECALCULATED` status, changed prior bytes, and a superseded current head. M7 consumes this receipt only with the complete validation context and displays the new version while retaining `MODEL_STALE`, `event_bound_model_validity_not_reconciled`, human review and `no_order`. A generic `ModelValidity=VALID` does **not** reconcile a reviewed ACTUAL material event.

The real 600519 descriptors still fail the complete-input gate: scenario inputs are not approved, and no real replacement valuation is registered. The two original ACTUAL material events remain `STILL_NOT_READY` with `new_valuation_result=null`; the synthetic success test is not a claim of a live 600519 valuation or a trading recommendation. The original WPS workbook remains byte-identical at SHA-256 `64c8deff1a237076d2ba0b00afc8905d23bd9d117cb132dfc6757071b5659911`.

The exact offline Core Gate most recently passed `694 passed, 18 skipped` with `M5_ACTUAL_EVIDENCE_ROOT=D:\GPTProject\value-investment` and a worktree-local pytest basetemp. The isolated v6 ACTUAL M7 candidate (real `STILL_NOT_READY` projection, **not** synthetic valuation numbers) has SHA-256 `c287020a95be26d79afd8f550d48a7770c89d76b993a543f3fce552b70f54e45`; read-only WPS/Excel COM verification passed with 10 visible and 2 hidden sheets, no formula errors, and no recommendation or order text. The verification receipt is local under `runtime/m7-v6-wps-receipt.json`. No production, scheduler, notification, account, PTA, or formal WPS publication action occurred.

Remaining engineering risk: the synthetic successful-refresh repository is in-memory; a persisted/reopened success receipt has not yet been replayed. Event-specific ModelValidity reconciliation also needs an explicit, evidenced contract before `RECALCULATED` may replace `MODEL_STALE`. The full specialized goal must not be marked complete on the strength of the synthetic path. Main-window integration should merge branch `codex/m5-actual-event-closure` from `origin/main` at `76e73b1` through this branch HEAD, preserving the real `valuation_inputs` blocker and `no_order` semantics.

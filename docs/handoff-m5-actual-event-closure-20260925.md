# M5 actual event replay handoff (2026-09-25)

## 2026-09-25 negative scenario review and replay update

The user reviewed the 600519 event-bound scenario package and returned
`NEED_MORE_EVIDENCE`. The append-only receipt
`docs/receipts/600519-event-bound-scenario-human-review-20260925.json` binds the
source review, proposal, two original event PDFs, ACTUAL receipt, graph, five-fact
artifact, pending descriptor and bounded result. All five scenario axes remain
`NOT_APPROVED`; four research blockers and eight `REOPEN_RESEARCH` triggers are
separate from the original `missing_dependency_node:valuation_inputs` blocker.
Both material events remain `STILL_NOT_READY`, with no model execution, new
valuation or order.

The reviewed M7 candidate now requires full ACTUAL evidence replay, not only a
read-model JSON and matching receipt hashes. Cross-worktree checkout exposed
CRLF conversion of the hash-bound research package and review receipt;
`.gitattributes` now pins both to LF, restoring their recorded SHA-256 bytes.
The replayed candidate is
`runtime/m7-actual-review-replayed-20260925.xlsx` (SHA-256
`a29d85dd951acdc2fcee2efe0989471cd3ec471145caad2c407634c602b529d0`).
WPS/Excel read-only verification passed: 10 visible and two hidden sheets, no
formula errors, no recommendation or order, and the formal workbook stayed at
SHA-256 `64c8deff1a237076d2ba0b00afc8905d23bd9d117cb132dfc6757071b5659911`.
The exact offline Core Gate file list passed `716 passed, 18 skipped` with the
main checkout supplied as the local ACTUAL evidence root. These checks do not
approve event-bound scenario inputs or a real refreshed valuation.

The visual-inspection successor
`runtime/m7-actual-review-replayed-v2-20260925.xlsx` (SHA-256
`4ed565e0c47a135a30994cf18e5f60135fcaa541038f8a249a61e78e5376418f`)
shows the four review blockers and eight evidence-reopen triggers as separate
rows with Chinese descriptions and original audit keys. The two event rows
remain `STILL_NOT_READY`. WPS read-only verification passed; it is still an
isolated candidate and does not replace the formal workbook.

This branch adds an offline, fail-closed replay for the 600519 actual event receipts. It verifies five half-year financial facts against archived PDF bytes and announcement metadata, builds a versioned fact artifact, derives a bounded recalculation plan, and projects the result into an M7 workbook candidate. No order, production database, scheduler, notification, or account action is performed.

The two actual receipts remain `STILL_NOT_READY`: `valuation_inputs` is missing, `model_executed=false`, and `new_valuation_result=null`. The workbook is a candidate, not the published WPS original or a trading recommendation. The current v5 candidate SHA-256 is `ff4a1412b41b313e347b9177207caab8a498a2bf04dd31742cfb1b2858fc460a`.

Verification in this isolated checkout: 22 targeted tests passed and 6 evidence-dependent tests skipped. Eight pre-existing outbox fixture failures were traced to Windows CRLF conversion; the fixture now retains LF bytes and those eight tests pass. The full repository suite is not green here (`2244 passed, 44 skipped, 208 failed, 87 errors`), principally because the isolated checkout lacks ignored `runtime` research evidence used by unrelated tests. Do not treat that run as production acceptance.

Follow-up binding audit: the result validator now checks exact active-event coverage and the status/blockers/task set implied by the frozen plan, even when a forged result is rehashed. Both the evaluator and M7 reader bind the facts artifact to the graph node's file-byte SHA and PDF evidence. An isolated replay regenerated two `STILL_NOT_READY` outcomes and a nine-review M7 read model; the replay files remain ignored under `runtime/`.

With `M5_ACTUAL_EVIDENCE_ROOT` pointing at the existing local evidence archive, the scoped M5/M7 regression is `34 passed` with no skips. This does not supersede the separate full-repository result above.

The M7 event sheet now exposes affected dependencies, explicit recalculation blockers, Event ID, human Decision Review requirement and the absence of a new valuation. A v5 candidate was rebuilt and read directly through openpyxl; WPS/Excel COM read-only acceptance passed. The canonical WPS workbook remained byte-identical at SHA-256 `64c8deff1a237076d2ba0b00afc8905d23bd9d117cb132dfc6757071b5659911`. The exact `core-research-gates.yml` offline test command passed locally: `686 passed, 18 skipped` with the existing local actual-evidence root available.

The `valuation_inputs` gap is substantive, not merely an adapter naming issue. The retained 600519 `valuation_result` predates these verified event facts. The archived `runtime/valuation-assumptions/600519-current-latest.json` points to an assumption package explicitly marked `mapping_only=true`, `valuation_recalculated=false`, and `valuation_approved=false`; it is not a new model-input artifact. The shared residual-income route requires authenticated starting book equity and ordinary shares plus complete scenario inputs. The five newly verified H1 fields do not include equity or shares, **but these values are not absent from the archive**: `runtime/company-research/600519-consolidated-parent-equity-inputs-latest.json` pins a separate 2026-H1 evidence package with parent equity `251253594419.50` CNY and issued shares `1250081601`, both referencing the same original PDF SHA-256 `0e10aa26be46b1cf3cd03f06e834c7fb98d5dd0d661b96f8fddd4af7e846a4f6`. Its evidence file SHA-256 is `efc4dc37d81e9d4bf54930a5c2bf7cd50305b7b7ce808531ef4bc4138f80891b`. That package says `valuation_approved=false` and `as_of_registry_verified=false`; the values have not been ingested into a new event-bound ResearchInputDescriptor. Reusing the prior result or relabeling the mapping-only package would break provenance.

Acceptance audit (candidate scope, not production authorization): A1 nine hash-bound review decisions are shown by the reviewed queue and M7 read model; A2 the input is an ACTUAL durable-run receipt, not a synthetic fixture; A3 the graph maps real `financial_facts` and `valuation_result` while retaining the actual `valuation_inputs` gap; A4 five versioned PDF-backed H1 facts exist; A5 two events have explicit bounded `STILL_NOT_READY` results, with no model execution; A6 M7 candidate exposes dependency, blocker, event and review state; A7 an isolated cold apply of the archived ACTUAL request matches the archived receipt fingerprint, and a second run after reopening stores is an idempotent replay; A8 scoped tests, Core Gate, WPS and workbook hashes pass, while the unrelated full repository suite is not green in the isolated checkout; A9 no order or position instruction is produced; A10 no production or PTA action was performed. The replacement valuation-input contract and model refresh remain open; this is not a claim that a usable new valuation exists.

The bounded-result evaluator now regenerates the entire plan from the receipt and graph before accepting it, so dropping an invalidated dependency while preserving a self-consistent result hash is rejected. A successful bounded refresh is still unimplemented: when complete authenticated inputs eventually exist, the existing ResearchApplication/ValuationRouter path must execute and persist a distinct new ValuationResult and ModelValidity; changing a status string is not a substitute. This keeps Engineering DONE unclaimed.

The M7 read model now decodes each review through the existing EventMaterialityDecision contract and compares the decision title, archived source reference, materiality status, affected fields/artifacts, cluster/supersede identity and direct dependencies against the actual event. Adversarial source-URL and affected-field edits are rejected. Rebuilding from the archived nine decisions still yields nine reviewed items; the read-model replay SHA-256 is `efbc63098619e51e16d9b91e4dd0086fab29795d346761d302513709e1f4667b` (local ignored runtime output).

The archived equity/share package was independently cross-checked with the newer M5 fact package and original PDF bytes. The current-period equity/prior-period equity pair appears on physical page 28; the opening/change/ending share row appears on physical page 22, and the closing equity also appears in the page-37 rollforward. This confirms those observations are available for a future descriptor, not that the existing unapproved assumptions can be promoted into a current ValuationResult.

The shared ResearchRunSpec now accepts a research case date later than its financial facts date, matching the pre-existing ResearchInputDescriptor PIT frame. It still requires the valuation date to equal the typed facts date, availability not to precede the research date, and the research date not to precede the facts. A typed descriptor test executes the existing ResearchApplication with distinct dates and confirms the new outcome retains the research date while the ValuationResult retains the older financial basis. This removes a contract-level impediment to a future event-bound descriptor; it does not create approved scenario inputs or a usable 600519 valuation.

An offline adapter now builds a blocked `ResearchInputDescriptor` from the ACTUAL receipt, PDF-bound verified facts and separately pinned equity/share archive. The local write-once candidate at `runtime/m5-pending-research-input-actual-20260925.json` has file SHA-256 `b7d658ff44e39fbecebd7d08dd1f95ce97114e95bd0a3e4ff61611543b3aebae` and descriptor input SHA-256 `72ad1453eec81d8c6b9a5908614928316f3f94007507dbfc276cb87100a031e6`. The existing ResearchApplication/ValuationRouter reads it and returns `not_ready` with null bear/base/bull, specifically because the event research and scenario inputs are unapproved. This candidate is **not** registered as a satisfied `valuation_inputs` dependency node; the original bounded result remains `STILL_NOT_READY`, and no new usable valuation or decision is published.

Next engineering gate: independently reconcile the archived equity/share observations to their PDF pages and connect them with the verified period facts in the existing `ResearchInputDescriptor` contract; obtain reviewed, PIT-consistent scenario assumptions without treating the mapping-only archive as approval. Route through the existing unified valuation interface; verify that any new result is tied to the actual event and facts before changing decision presentation. Until then `STILL_NOT_READY` and no new valuation are the correct outcome. Keep the formal workbook unchanged.

The pending-input adapter now requires the verified facts' PDF SHA-256, source URL and announcement ID to identify exactly one active event in the ACTUAL receipt. Three rehashed facts-package tampering cases are rejected; a same-symbol but unrelated filing can no longer enter this preflight. Commit `7eea874` contains the scoped change. The workflow's offline Core Gate passed `689 passed, 18 skipped` with `M5_ACTUAL_EVIDENCE_ROOT=D:\GPTProject\value-investment` and pytest's temporary directory explicitly placed under this isolated worktree. The default Windows temp directory was inaccessible (`WinError 5`); the initial `150 errors` were fixture setup errors, not test assertion failures. The formal workbook and original bounded `STILL_NOT_READY` result are unchanged. A successful bounded refresh remains unimplemented and cannot be inferred from this gate.

## Subsequent bounded-refresh engineering

The last sentence above is superseded for the **synthetic complete-input path only**. Commits `c5ac839` through `756c06d` compute source hashes from exact bytes, reject mismatched typed-facts/thesis graph bindings, attach a `valuation_inputs` node only after event/PDF/receipt and model-input preflight checks, and execute a bounded refresh through the existing `ResearchApplicationService`. The result receipt pins the frozen plan, prior source-file bytes, prior persisted artifact, new persisted valuation version and current repository head. Its validator rejects a rehashed false `RECALCULATED` status, changed prior bytes, and a superseded current head. M7 consumes this receipt only with the complete validation context and displays the new version while retaining `MODEL_STALE`, `event_bound_model_validity_not_reconciled`, human review and `no_order`. A generic `ModelValidity=VALID` does **not** reconcile a reviewed ACTUAL material event.

The real 600519 descriptors still fail the complete-input gate: scenario inputs are not approved, and no real replacement valuation is registered. The two original ACTUAL material events remain `STILL_NOT_READY` with `new_valuation_result=null`; the synthetic success test is not a claim of a live 600519 valuation or a trading recommendation. The original WPS workbook remains byte-identical at SHA-256 `64c8deff1a237076d2ba0b00afc8905d23bd9d117cb132dfc6757071b5659911`.

The exact offline Core Gate most recently passed `694 passed, 18 skipped` with `M5_ACTUAL_EVIDENCE_ROOT=D:\GPTProject\value-investment` and a worktree-local pytest basetemp. The isolated v6 ACTUAL M7 candidate (real `STILL_NOT_READY` projection, **not** synthetic valuation numbers) has SHA-256 `c287020a95be26d79afd8f550d48a7770c89d76b993a543f3fce552b70f54e45`; read-only WPS/Excel COM verification passed with 10 visible and 2 hidden sheets, no formula errors, and no recommendation or order text. The verification receipt is local under `runtime/m7-v6-wps-receipt.json`. No production, scheduler, notification, account, PTA, or formal WPS publication action occurred.

The synthetic success test now writes its immutable research-artifact envelopes to a temporary JSON bundle, restores them into a fresh repository instance, and verifies that the same bounded-refresh receipt and M7 read model still validate identically. This is an isolated artifact-bundle cold replay, **not** a disposable PostgreSQL durability/transaction test. Event-specific ModelValidity reconciliation still needs an explicit, evidenced contract before `RECALCULATED` may replace `MODEL_STALE`. The full specialized goal must not be marked complete on the strength of the synthetic path. Main-window integration should merge branch `codex/m5-actual-event-closure` from `origin/main` at `76e73b1` through this branch HEAD, preserving the real `valuation_inputs` blocker and `no_order` semantics.

A later acceptance audit found that an apparently complete synthetic descriptor did not prove every value actually consumed by the model. The registration gate now requires every bear/base/bull residual-income leaf (cost of equity, each forecast ROE, terminal ROE, terminal growth and retention) to match an individually evidenced assumption whose SHA is pinned in the descriptor sources. It also reads the issuer operating-basis artifact from exact bytes and compares the model's starting parent equity and issued shares against that package, its report date, source PDF, assessment availability and source SHA. A changed equity amount remains rejected even after rehashing and re-pinning the package. These are profile-level rules, not a 600519 symbol branch. The real input remains blocked for unapproved scenario assumptions; the synthetic complete-input test uses explicit test-only assumptions and must not be mistaken for a research conclusion. The offline Core Gate after these changes remains `694 passed, 18 skipped`.

The complete-input gate now also checks exact assumption-package bytes against the typed assumption set and a descriptor-pinned SHA (`9063802`). A forged package with a recomputed source SHA is rejected when its contents disagree with the model inputs. The bounded-refresh validator independently recomputes the complete valuation payload from the frozen descriptor using the registered shared model and rejects a separately persisted, self-consistently rehashed valuation whose base value was changed. The real 600519 route remains `STILL_NOT_READY`; no actual assumption approval, event-specific ModelValidity reconciliation, or formal workbook publication followed from these synthetic checks. The exact offline Core Gate is `694 passed, 18 skipped`; the formal WPS workbook SHA-256 remains `64c8deff1a237076d2ba0b00afc8905d23bd9d117cb132dfc6757071b5659911`.

The bounded-result evaluator now requires the verified facts' PDF SHA, URL and complete announcement-ID token to match exactly one active ACTUAL event, rather than accepting any same-symbol facts package. The pending-input adapter uses the same complete-token rule; a shortened announcement ID no longer passes as a substring. This applies to both explicit `STILL_NOT_READY` results and the complete-input refresh preflight. The real 600519 filing continues to match its archived ACTUAL event and remains `STILL_NOT_READY` because reviewed scenario inputs are absent. Event/facts targeted regression: `36 passed`; offline Core Gate: `696 passed, 18 skipped`. No formal workbook or production state was changed.

The standalone `STILL_NOT_READY` result validator now requires the frozen graph, verified facts artifact and facts-file SHA, then regenerates the expected result from those inputs. Previously a rehashed result could substitute another graph or facts payload SHA while preserving internally consistent task statuses. M7 supplies this evidence before displaying an offline blocked outcome. Two adversarial rehash cases and the real ACTUAL read-model replay pass; targeted regression `32 passed`, offline Core Gate `698 passed, 18 skipped`. This hardens replay provenance but does not approve missing real valuation assumptions or convert `STILL_NOT_READY` into a refreshed valuation.

The M7 read model now requires one-to-one coverage of every active ACTUAL event by reviewed rows and rejects duplicate `event_decision_id` values across the queue. A receipt with an extra invalidated event but no matching reviewed row is rejected even when its bounded plan is rebuilt consistently; it cannot silently disappear from the workbook view. The real nine-review/two-material-event replay remains valid. Offline Core Gate: `700 passed, 18 skipped`. The formal WPS original is still outside this candidate-only work.

A read-only regression now reopens the archived `1225475868.pdf`, runs the deterministic half-year fact verifier again, and compares the reconstructed five-fact payload, identity, evidence refs and payload SHA against the frozen ACTUAL artifact. It passed with the local archived evidence root; when that root is explicitly supplied, missing inputs fail rather than silently skip. This is stronger A4 evidence than merely trusting the stored facts JSON. It does not create new assumptions or a valuation. The offline Core Gate is `701 passed, 18 skipped`.

## Current offline acceptance audit

The current branch was rechecked against `origin/main` at `76e73b1`. The latest ACTUAL CLI read-model replay produced `runtime/m5-actual-read-model-acceptance-20260925.json` (SHA-256 `e30442faa3b59183df24a12748e24aaa41ae6c09b6cdf842331c31f032568bbc`): nine reviewed disclosures, five PDF-verified facts, two material events `STILL_NOT_READY`, and no new valuation. The isolated v7 M7 candidate is `A股价值投资_M7真实事件工作台候选_v7_20260925.xlsx` (SHA-256 `67fe15d0c5e32a64cf6f7fc77eba16ba6d396ab001b37848b527536aa09742c8`). Read-only WPS verification passed with ten visible and two hidden sheets, no formula errors, no order recommendation and the formal canonical unchanged at `64c8deff1a237076d2ba0b00afc8905d23bd9d117cb132dfc6757071b5659911`. Receipt: `runtime/m7-v7-wps-receipt.json`.

- A1-A2: archived nine-review manifest and real ACTUAL durable request/receipt remain the source; the isolated cold replay matches its archived audit fingerprint and is idempotent after store reopen.
- A3-A4: the graph contains real facts and prior valuation nodes, preserves the missing `valuation_inputs` node, and the original half-year PDF regenerates the exact five-fact artifact.
- A5: both material events have an explicit, validated bounded `STILL_NOT_READY` result; this is the allowed blocked outcome, not a completed valuation recalculation. The synthetic complete-input path exercises the shared valuation service but does not approve real assumptions.
- A6-A7: the M7 candidate displays the two event-level blockers and null new valuation; cold read-model replay covers all active events with unchanged status semantics.
- A8: all 25 M5 test files passed (`207 passed`), the exact offline Core Gate is `701 passed, 18 skipped`, the final actual replay subset is `16 passed`, and v7 WPS read-only verification passed. The older isolated full-repository run was not green because unrelated ignored runtime evidence was absent; it is not used as proof here.
- A9-A10: all actual outcomes and the candidate remain `no_order`; no production database, scheduler, notification, account, PTA, server or formal WPS action was performed.

`SPECIALIZED_GOAL_STATUS = PARTIAL_WITH_VALIDATED_NOT_READY`. Engineering DONE for a **real refreshed valuation** is not claimed. Next DAG node: reviewed, PIT-consistent scenario assumptions and an event-bound complete `valuation_inputs` descriptor for 600519. A future successful model execution also needs an explicit event-specific ModelValidity reconciliation before `MODEL_STALE` can become `RECALCULATED`. Neither should be fabricated from the mapping-only archived assumptions or inferred from the synthetic test. Main-window integration should merge this branch through its latest HEAD while preserving these blockers.

The pending 600519 research-input adapter was subsequently checked against the complete-input registration gate. Its earlier candidate pinned only the half-year filing PDF, omitting the separate 2026-07-18 pricing announcement; it also cited the ACTUAL receipt in the case but omitted its state SHA from descriptor sources. The generic adapter now requires one matching source PDF per active ACTUAL event and registers all event PDFs plus the receipt state SHA. The real write-once candidate is `runtime/m5-pending-research-input-complete-sources-20260925.json` (file SHA-256 `2a53dd84b652ab0e0b3997dd1d764bf37d891ce4fde19884f61564914a9bf5d5`, descriptor input SHA-256 `4e4bae0e877e599f5d54b17c4fcec0909752005f87b2adeebaaff320697105fd`). Its five sources include the half-year PDF (`0e10aa26...46a4f6`), pricing PDF (`24e51c43...92873`), verified facts, issuer equity basis and ACTUAL receipt. It still has null assumptions/scenarios, `model_executed=false`, `action=no_order`, and blockers `m5_actual_event_research_pending` and `scenario_inputs_not_approved`. The structural source gap is fixed; price-volume/margin response and H1 forward scenarios remain unapproved research judgments. Targeted tests: `20 passed`; exact offline Core Gate: `702 passed, 18 skipped`. The formal WPS workbook remains byte-identical.

A generic `attach_completed_event_research_input` boundary now checks the hash-bound pending and completed descriptor versions, monotone PIT, unchanged issuer operating basis, exact preservation of every pending source and research evidence reference, and the pending receipt/all active-event PDFs. It then delegates model-input registration to the existing complete-input graph gate. The synthetic complete-input path exercises successful attachment and rejects rehashed drops of the pending event PDF, a source or a research-case evidence reference. This is an engineering upgrade path only: the actual 600519 pending descriptor is not promoted, has no approved scenario inputs, and still produces no new valuation. Targeted tests: `20 passed`; offline Core Gate: `702 passed, 18 skipped`.

The A1 source chain was rechecked against the original archive, not just a queue JSON: all nine reviewed disclosure PDF files exist under the pinned root and their byte SHA-256 values equal their individual materiality decisions. The delegated application manifest also matches the exact review-file and queue-file bytes. This check is now a real-evidence regression in `test_m5_actual_read_model.py`; together with actual durable cold replay and the half-year PDF fact replay, the focused subset is `17 passed`. The exact offline Core Gate is `703 passed, 18 skipped`. The two material outcomes remain `STILL_NOT_READY` with null replacement valuation. No real scenario approval or event-specific ModelValidity reconciliation has been asserted.

Commit `a2df853` adds the optional event-specific refresh-review contract. For a completed bounded refresh, M7 may show `RECALCULATED` only when a hash-bound human research-lead review covers every active material event, binds the original materiality decisions, PDFs, receipt, plan, facts, assumptions and new valuation, and the persisted generic ModelValidity is `VALID`. The read model still requires human decision review and `action=no_order`; without accepted review it remains `MODEL_STALE`. This is exercised with synthetic complete inputs only. A blocked real result cannot carry such a review, so both 600519 events remain `STILL_NOT_READY` and the formal workbook remains unchanged. Focused regression: `39 passed, 3 skipped`; exact offline Core Gate: `705 passed, 18 skipped`. The canonical WPS SHA-256 was rechecked as `64c8deff1a237076d2ba0b00afc8905d23bd9d117cb132dfc6757071b5659911`. The outstanding real DAG node is approved, PIT-consistent event-bound scenario inputs; no real new valuation or trading instruction is claimed.

The later `fa0dfe5` M7 guard rejects a purported `RECALCULATED` row without an ACTUAL Event ID; Core Gate remains `705 passed, 18 skipped`. A check of the archived forward-assumption package at `runtime/company-research/600519-current-forward-assumptions-20260924T090504Z/evidence.json` (SHA-256 `57929708314a9255d02453aefb4053575ff8b1918d4506ee197ac777e2041f63`) found `passed=true` only for bounded conditional research, while `valuation_approved=false`, `simulation_eligible=false` and `trade_approved=false`. Its limitations explicitly require a new policy review after a new disclosure. It therefore cannot satisfy this ACTUAL event's `valuation_inputs` node. The remaining gate is a reviewed, event-bound scenario/model-input package and, if it supports a legitimate refresh, a separate human event-reconciliation review. Until those judgments exist, the verified `STILL_NOT_READY` outcome is the final offline research state, not an implementation or data-fetch failure.

## 2026-09-25 main integration and M7 candidate v9

The M5 branch's reviewed ACTUAL read-model binding and scannable research blockers were integrated into `main`. The latest candidate, `runtime/m7-actual-reviewed-candidate-v9-20260925.xlsx`, was rebuilt from the nine-review ACTUAL evidence set, the five PDF-verified facts, the bounded result, and the negative scenario review. Its SHA-256 is `76c3465c02823b3799c65173257d84953a6385d8c0daa5081f5ee04e4783f8c3`; its manifest SHA-256 is `002f85e0f4644640d8d18fcc76ec4e5126bfe9b12bb5715e6a56d0b383699e15`. Read-only WPS verification passed at `runtime/m7-actual-reviewed-candidate-v9-wps-receipt.json`: ten visible and two hidden sheets, no formula errors or order instructions, and the formal WPS original unchanged at SHA-256 `64c8deff1a237076d2ba0b00afc8905d23bd9d117cb132dfc6757071b5659911`.

The overview distinguishes 600519's nine reviewed disclosures (zero pending, two `STILL_NOT_READY`) from a separate M5 queue item for 600887 (one pending). The two 600519 events still have `model_executed=false`, `new_valuation_result=null`, and `action=no_order`. The candidate is not published to the formal WPS workbook. The outstanding research node remains reviewed, PIT-consistent event-bound scenario inputs; the existing `NEED_MORE_EVIDENCE` decision is not approval to fabricate them.

The current `main` M5/M7 suite ran `245 passed` with the original announcement archive and the isolated verified-facts artifacts supplied as two explicit evidence roots; missing explicitly supplied roots now fail instead of silently skipping the ACTUAL replay. A fresh, local CNINFO scan for 600519 covering 2026-09-10 through 2026-09-25 returned zero announcements, with queue coverage marked `COMPLETE` and no source failure. Its archived index SHA-256 is `1640d624cdfe8dd86795e7598eda55336d07adb927dba60efff400e472e4a0b8`; queue SHA-256 is `c8cb742fbf2b48384cdd5cd3b2e4a9d8c034f1b8a3af280dae69068e6ff2ad55`. This is a bounded, single-provider observation, not proof that no other post-event operating evidence exists. It does not reopen or approve the rejected scenario inputs.

The M7 v10 candidate `runtime/m7-actual-reviewed-candidate-v10-20260925.xlsx` now binds that follow-up queue and its archived index, requiring a gap-free continuation from the reviewed queue's 2026-09-09 cutoff, exact 600519 identity, complete zero-announcement coverage, source-index Hash and a nonfuture observation. A nonzero, missing, tampered or discontinuous scan cannot use this zero-announcement display path. The overview and event sheet show the 2026-09-25 CNINFO cutoff and explicitly warn that a single-provider zero result is not sufficient operating evidence. Candidate SHA-256: `66660b86fb56954dd3a86833b49121fe8235db78f8ac1c4ace6b80fdfadf6da1`; manifest SHA-256: `bb8bb3468fea98cd4762f26747663cdc0308fff5e9155c2bb68916aca94d06c0`. The WPS read-only receipt is `runtime/m7-actual-reviewed-candidate-v10-wps-receipt.json` (`passed`, 10 visible/2 hidden, `no_order`); the formal WPS original remains SHA-256 `64c8deff1a237076d2ba0b00afc8905d23bd9d117cb132dfc6757071b5659911`. The new follow-up test is in Core Gate (`721 passed, 18 skipped` in the isolated checkout). Both material events still have no refreshed valuation and remain `STILL_NOT_READY`.

## Final scoped engineering acceptance

At `main` HEAD `5573715`, the specialized A1-A10 audit supports **Engineering DONE for the permitted bounded NOT_READY outcome**, not a refreshed valuation or investment readiness:

- A1-A2: the nine-decision review file (`46356cf8...0b9966`) binds archived PDF bytes; the ACTUAL request cold-applies to isolated durable stores and reproduces the archived receipt (`b93ea107...6a31b56`), then reopens idempotently with the same fingerprint.
- A3-A5: the versioned graph (`9e14506c...1ad3531`), five-fact PDF-backed artifact (`3481dd17...a47c7862`), plan (`d4fb1a5d...31718088`) and hashed bounded result (`0daf8d45...13f6fa`) preserve the real missing `valuation_inputs` node. Both material events end `STILL_NOT_READY`, `model_executed=false`, `new_valuation_result=null`; unverified candidates remain pending and are not model facts.
- A6-A8: M7 v10 displays the event/dependency/blocker chain and reviewed research state without promoting a valuation. Current-main M5/M7 suite: `248 passed`; Core Gate: `739 passed`, with explicit ACTUAL evidence roots. The v10 WPS read-only receipt and freshly recomputed candidate/canonical hashes match; no formula errors or trade instructions were found.
- A9-A10: the read model and workbook remain `action=no_order`, with no BUY/ADD, target weight or order. This scoped work did not deploy production services, enable a scheduler or notification, access accounts, or modify PTA. The formal WPS original was not replaced.

`SPECIALIZED_GOAL_STATUS = ENGINEERING_DONE_WITH_VALIDATED_NOT_READY`. The separate research node `valuation_inputs` remains `NEED_MORE_EVIDENCE` / R6: post-July target-SKU volume, channel mix, realized margin, parent distribution capacity, dated discount inputs and terminal-ROE evidence must support independently reviewed scenario leaves before a new valuation can be registered. A subsequent legal refresh would additionally need event-specific ModelValidity reconciliation and still cannot authorize a real-money action. The single-provider CNINFO zero-announcement follow-up does not satisfy those inputs.

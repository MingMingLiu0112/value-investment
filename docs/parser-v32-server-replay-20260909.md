# v32服务器运行环境重放

2026-09-09，使用生产容器镜像、生产只读依赖、服务器已有原PDF，优先加载暂存的四个发布模块。没有挂载数据库凭据，没有写数据库，没有替换生产代码。

## 已验证结果

- 固定计划：`runtime/candidate-release-plan-20260908T210613655180Z.json`。
- 固定生产清单：`runtime/candidate-release-inventory-20260908T210112717345Z.json`。
- 发布包：`runtime/parser-release-v32-20260909/manifest.json`，记录四文件前后SHA-256。
- 20份财报的服务器PDF Hash与计划一致，275条候选的字段、来源标签、Decimal数值、单位、页码、单位依据页及原文，与本地结果逐项多重集合比较一致。
- 深圳能源12条候选使用跨页单位声明，候选入库摘录辅助函数保留单位原文。
- 候选复核模块和数据库模块能够与未修改的生产依赖共同导入。
- 0.5 CPU、384MiB内存/512MiB含交换上限，既有共享任务锁；执行成功退出。

## 首次尝试失败及修正

首次使用四个单文件覆盖挂载，但生产只读目录尚不存在 `combined_balance.py`，容器创建挂载点失败，未执行Python。修正为暂存目录优先的包搜索路径，其他依赖继续来自生产只读目录；脚本检查实际加载模块SHA-256，防止意外仍使用旧文件。修正后的完整重放成功。

## 尚未完成

这不是正式代码发布或候选晋升。275条是候选数量，既不是新增已验证数据点数量，也不是新增公司数量。后续须受控部署、重提取及双源验证，显式补回旧证据摘录，并刷新原Excel。真实历史策略回测仍未完成。

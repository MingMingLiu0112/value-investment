# v32生产发布记录

2026-09-09，四文件发布已完成，并从实际生产路径完成20份财报重放验证。不是仅验证暂存文件。

## 发布范围

- `filing_extract.py`：v32显式报告单位及已审查解析修正。
- `combined_balance.py`：新增辅助解析模块。
- `db.py`：以生产版本为基础，仅增加单位证据摘录辅助函数并修改候选入库摘录调用。未覆盖本地其他数据库改动。
- `candidate_review.py`：只新增两处候选原文摘录保留。

精确前后Hash见 `runtime/parser-release-v32-20260909/manifest.json`。
生产回执已下载：`runtime/parser-v32-installed-receipt-20260909.json`。
服务器备份：`/opt/value-investment-agent/deploy-staging/parser-v32-backup-20260909`。

## 验证及运行边界

安装器5项专项测试通过，覆盖生产变更拒绝、安装成功、验证失败时旧文件恢复和新增模块移除。实际安装使用共享任务锁，逐文件前后Hash核对、旧文件备份、原子替换和失败回滚。安装后原始财报重放使用0.5 CPU、384MiB内存/512MiB含交换上限，无数据库凭据。

首次默认系统Python过旧，编译前置检查拒绝，未写生产文件。改用服务器已存在的Python3.11.9执行安装，不改变默认Python。

正式安装后从生产路径加载四文件，20份原PDF Hash一致，275条候选的字段、标签、数值、单位、页码及单位证据与固定本地计划一致；12条跨页单位证据保留。生产重放脚本使用 `--installed`，不启用暂存模块搜索路径。

## 未完成事项

此发布只变更代码，不写数据库或Excel，不代表275条候选已通过双源验证。旧候选摘录不会因INSERT ON CONFLICT DO NOTHING自动补回，需要显式审计回填。20份已审查报告仍需受控重提取、候选入库和验证，96条已退役候选禁止恢复、8条有效旧附注保留。全市场其他报告不是本次20份精确比较的验收范围。

没有重启或修改PTA、Hermes、PostgreSQL服务配置。没有新增大批PDF下载、磁盘扩容或降低保留线。真实历史交易策略回测尚未完成。
